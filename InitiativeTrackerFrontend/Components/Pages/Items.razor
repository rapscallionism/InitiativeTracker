@page "/items";
@using InitiativeTracker.Components
@using MudBlazor

<PageTitle>Items</PageTitle>

<MudText Typo="Typo.h3">Items</MudText>

<MudTextField 
    @bind-Value="SearchedEquipment" 
    Label="Standard" 
    Variant="Variant.Outlined" 
    Adornment="Adornment.End" 
    AdornmentIcon="@Icons.Material.Filled.Search" />

@code {
    // Page is supposed to be about for custom items to be able to keep track of the users' items for us and resets on short rest and long rest
    // List<Item> items = ItemParser.ParseAllJsonToItems("../../ItemsJson");

    // [Parameter]
    // public 

    public string? SearchedEquipment { get; set; } = string.Empty;
    public string? Search { get; set; } = string.Empty;

    protected override void OnInitialized() {
        Console.WriteLine("Running!");
    }

    protected override Task OnInitializedAsync()
    {
        return Task.Run(() => Task.CompletedTask);
        // On initial render, call the users api to get all items
    }
}